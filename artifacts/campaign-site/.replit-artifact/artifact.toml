kind = "web"
previewPath = "/"
title = "Eating Disorder Awareness Campaign"
version = "1.0.0"
id = "artifacts/campaign-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23591

[services.development]
run = "pnpm --filter @workspace/campaign-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/campaign-site", "run", "build" ]
publicDir = "artifacts/campaign-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23591"
BASE_PATH = "/"
